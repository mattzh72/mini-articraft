# Minimal local stubs for the pxr (usd-core) surface this repo uses.
# usd-core ships no type information; these stubs declare the module-level
# names we call so basedpyright can check them, while instance members stay
# dynamic (each class defines ``__getattr__``). Extend them as usage grows.
